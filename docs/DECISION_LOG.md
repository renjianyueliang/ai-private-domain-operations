# 决策日志

## 2026-07-29

- 仓库文档、`STATUS.md` 和 `TASK_QUEUE.json` 作为长期事实源，聊天上下文不再承担项目记忆。
- AI 团队单次最多推进一个任务，风险门和依赖校验先于实现。
- 阶段 1–2 只建立基线、验收与只读控制器；无人值守写入执行器推迟到阶段 3。
- 自动部署保持关闭；分支、验证和草稿 PR 是当前最远交付点。
- 客户端现阶段只能称为“本地演示闭环可验证”，不能称为真实商业闭环已经跑通。
- 真实渠道、客户触达、账号、密钥、支付和高风险内容始终保留人工批准。
- 用户批准第三阶段受控自动化；使用 Codex Automation 的 linked worktree，不尝试绕过 WindowsApps 对桌面 Codex executable 的访问限制。
- 阶段 3 允许队列中的 `low_risk_write` 自动运行，但禁止自动修改控制面、提交、推送、创建 PR、合并和部署。
- 用户要求直接进入 AI 团队持续开发；阶段 4 允许验证通过的低风险任务受控更新状态、提交并普通推送到现有草稿 PR 分支。
- 自动合并、主分支写入、部署、真实渠道与客户动作继续禁止；远端竞争时停止，禁止 force push。
