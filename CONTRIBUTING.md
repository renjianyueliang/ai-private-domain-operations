# 贡献指南

感谢你愿意改进 AI 私域 SaaS 指挥官。项目当前优先接受可验证、低风险、边界清晰的小步贡献。

## 开始之前

1. 先搜索现有 Issues，确认没有重复问题。
2. 对涉及产品方向、数据结构、外部平台接入或较大重构的改动，先创建 Issue 说明场景、方案和风险。
3. 不要提交真实客户数据、访问令牌、密钥、手机号、私信记录或其他敏感信息。

## 本地开发

```powershell
npm install
npm run typecheck
npm run build
```

复制 `.env.example` 为 `.env.local` 可启用可选集成。默认 mock 模式不需要外部账号。

## Pull Request 要求

- 每个 PR 聚焦一个问题，并说明用户价值、改动范围和验证结果。
- 保留高风险内容与对外触达的人工确认边界。
- 新功能应补充可复现步骤或验收清单；修复应描述根因和回归验证。
- 不绕过平台规则，不接入个人号外挂，不加入批量骚扰、自动成交承诺或无审核的医疗/金融建议。
- 提交前运行 `npm run typecheck` 和 `npm run build`。

## Issue 建议

缺陷报告请包含：使用路径、预期结果、实际结果、复现步骤、运行环境和已脱敏截图。功能建议请优先说明具体运营角色、触发场景、现有阻力和可验收结果。

参与本项目即表示你同意遵守 [行为准则](CODE_OF_CONDUCT.md)，并同意你的贡献按 [AGPL-3.0](LICENSE) 发布。
